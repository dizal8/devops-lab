apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lab01-wildcard
  namespace: default
spec:
  secretName: lab01-wildcard-tls
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - "*.lab01.dzl.ro"
    - "lab01.dzl.ro"
